name: site-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - jupyter
  - ipykernel
  - pyyaml
  - nbformat
  - nbclient
  - pip
  - pip:
      - quarto
      - -e /Users/johnmyers/dev/sigalg[dev]
      - seaborn
